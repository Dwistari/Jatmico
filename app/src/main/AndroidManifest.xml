<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.dwi.jatmico"
          android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.home.HomeActivity">
        </activity>
        <activity android:name=".view.my_submission.MySubmissionActivity">
        </activity>
        <activity android:name=".view.isues.IssuesActivity">
        </activity>
        <activity android:name=".view.search.SearchActivity">
        </activity>
        <activity android:name=".view.detail_isues.DetailIssueActivity">
        </activity>
        <activity android:name=".view.create.CreateIssueActivity">
        </activity>
    </application>

</manifest>